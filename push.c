#include "monty.h"

/* Function to push an element onto the stack */
void f_push(stack_t **head, unsigned int number) {
    addnode(head, number); /* Assuming addnode function adds a node to the stack with the given number */
}
